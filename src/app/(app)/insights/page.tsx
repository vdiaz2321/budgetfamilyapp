import PagePlaceholder from "../page-placeholder";

export const metadata = { title: "Insights · Capitall" };

export default function InsightsPage() {
  return (
    <PagePlaceholder
      title="Insights"
      step="Step 8"
      blurb="See where every dollar goes — spending by category, spending over time, income trends, and income vs. spent — filterable by month, year, or year-to-date."
    />
  );
}
