import PagePlaceholder from "../page-placeholder";

export const metadata = { title: "Accounts · Capitall" };

export default function AccountsPage() {
  return (
    <PagePlaceholder
      title="Accounts"
      step="Step 4"
      blurb="Every account in one place — checking, savings, credit cards, investments, stocks, and family investments. Balances entered here feed Networth automatically, so you never re-type them."
    />
  );
}
