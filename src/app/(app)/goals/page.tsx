import PagePlaceholder from "../page-placeholder";

export const metadata = { title: "Goals · Capitall" };

export default function GoalsPage() {
  return (
    <PagePlaceholder
      title="Goals"
      step="Step 9"
      blurb="A light snapshot of the family — names, ages, grades, expected high-school and college graduation, and the places you've lived or want to live."
    />
  );
}
